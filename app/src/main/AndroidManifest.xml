<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ALL PERMISSIONS GO HERE - OUTSIDE APPLICATION TAG -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Librium"
        android:largeHeap="true"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Librium.Immersive">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Wellness Dashboard -->
        <activity
            android:name=".WellnessActivity"
            android:theme="@style/Theme.Librium.Immersive" />

        <!-- Schedule View -->
        <activity
            android:name=".ScheduleActivity"
            android:theme="@style/Theme.Librium.Immersive" />

        <!-- Communication Hub (Email + Contacts) -->
        <activity
            android:name=".CommunicationActivity"
            android:theme="@style/Theme.Librium.Immersive" />

    </application>

</manifest>